package com.example.myapplication.ui.screens

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

@Composable
fun SettingsScreen() {
    // Пустой экран
    Text(text = "Settings Screen") // Можно убрать, если нужен совершенно пустой экран
}
